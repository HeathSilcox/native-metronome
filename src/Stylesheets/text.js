import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    whiteBold: {
        color: '#fff',
        fontWeight: 'bold',
    },
    large: {
        fontSize: 18,
    },
    center: {
        textAlign: 'center',
    }
});